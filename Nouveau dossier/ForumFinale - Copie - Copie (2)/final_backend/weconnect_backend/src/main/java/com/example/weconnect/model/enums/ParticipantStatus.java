package com.example.weconnect.model.enums;

/**
 * Enum representing the status of a participant in an event or event club.
 */
public enum ParticipantStatus {
    CONFIRMED,   // Participation is confirmed
    PENDING,     // Participation is pending approval
    CANCELLED    // Participation has been cancelled
}
